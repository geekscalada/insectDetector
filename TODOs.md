quizás tiene sentido hacer un flujo de openspec primero, sin agentes, para un proyecto dummy y ver cómo funciona todo el flujo, pasarle primero
el documento de gemini donde se explica como se usa

crear unos agentes para python y CV con los que podamos testear que funciona como se espera agentes con orquestación + openspec. 
openspec init y seguir el flujo de open-spec pasándole el diseño inicial que hemos preparado usando mi agente orquestador para que delege en agentes implementadores...
probar openspec con mi orquestación de agentes


